# Core
streamlit>=1.32.0              # legacy UI (kept during React migration)
python-dotenv>=1.0.0           # environment variables

# React backend (FastAPI)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9        # file uploads
httpx>=0.27.0                  # backend tests (httpx.AsyncClient)

# Google Gemini + ADK
google-genai>=1.0.0            # new unified Gemini API SDK (replaces deprecated google-generativeai)
google-adk>=1.30.0             # Agent Development Kit — structured function calling, no regex parsing

# Geospatial
geopandas>=1.0.0               # flexible version (uses pyogrio by default, no fiona needed)
shapely>=2.0.0                 # geometry operations  
folium>=0.15.1                 # map visualization  
streamlit-folium>=0.18.0       # streamlit integration  
pyproj>=3.5.0                  # coordinate transformations
pyogrio>=0.7.0                 # fast geospatial I/O (replaces fiona)
# rasterio>=1.3.0              # OPTIONAL: raster/satellite imagery (may need manual install on Windows)

# Optimization
spopt>=0.5.0                    # spatial optimization  
libpysal>=4.10.0                # spatial data structures (lighter than full pysal)
# pysal>=24.1                   # OPTIONAL: full pysal suite (requires GDAL/fiona on Windows)
# gurobipy>=11.0.0              # OPTIONAL: install manually if you have 
pulp>=2.8.0                     # linear programming solver  
scipy>=1.12.0                   # scientific computing  
networkx>=3.2.1                 # network analysis

# Data processing
hdx-python-api>=6.0.0            # HDX API client for population data
pandas>=2.2.0                    # allow upgrades but be cautious with breaking changes  
numpy>=1.26.4                    # allow newer patches  
openpyxl>=3.1.2                  # allow minor updates
Pillow>=10.0.0                   # for raster image processing  

# Visualization
matplotlib>=3.8.3                # allow newer patches  
seaborn>=0.13.2                  # allow newer  
plotly>=5.19.0                   # allow minor upgrades  
branca>=0.7.1                    # for folium compatibility  

# PDF generation
reportlab>=4.1.0                 # allow newer patches  
pypdf2>=3.0.1                    # allow minor updates  

# Testing
pytest>=8.0.2                     # allow newer  
pytest-cov>=4.1.0                # allow newer  

# Performance monitoring
psutil>=5.9.0                    # system metrics monitoring

overturemaps>=0.7.0              # Overture Maps cloud-native data (boundaries, POIs)
pyarrow>=14.0.0                  # Required by overturemaps (Parquet/Arrow I/O)

# Future enhancements (optional - may require additional system dependencies)
osmnx>=1.9.1                    # OpenStreetMap road-network analysis (network distance metric)
# contextily>=1.5.2              # OPTIONAL: basemap tiles
# h3>=4.0.0                      # OPTIONAL: H3 hexagonal grid for population demand points
